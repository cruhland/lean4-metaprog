On `generic` branch:
1. Make `metavariableExample` generic over `MyMetaM M`
1. Make `MyMetaM MetaM` instance more efficient by replacing operations with
   more low-level ones.
1. Finish Chapter 4
1. Merge branch into `main`
