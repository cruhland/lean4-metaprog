On `generic` branch:
1. Generic impl of `Lean.Meta.isDefEq`
1. Generic type for `LocalContext`
1. Generic impl of `Lean.getLCtx`
1. Generic impl of `ForIn` for `MyLocalContext`
1. Generic impl of `MVarId.withContext`
1. Replace `Lean.MVarId` in argument type of `myAssumption` with `MyMVarId`
1. Replace `MetaM` in `myAssumption` with `MyMetaM`
1. Finish _Local contexts_ section
1. Finish Chapter 4
1. Merge branch into `main`
