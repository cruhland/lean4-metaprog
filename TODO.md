On `generic` branch:
1. Remove all type parameters from `MyExpr`
1. Consider not using a generic `MyLevel` for `MyExpr` at all, due to stuck
   metavars
1. Finish Chapter 4
1. Merge branch into `main`
